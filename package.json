{
  "name": "saasquatch",
  "version": "0.1.0",
  "private": true,
  "description": "Production-ready CLI tool that generates complete microservices architectures using Fastify",
  "author": "SaaSQuatch Team",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean && rimraf node_modules",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "@jest/globals": "^30.2.0",
    "@types/fs-extra": "^11.0.4",
    "@types/jest": "^30.0.0",
    "@types/node": "^20.10.0",
    "husky": "^9.0.11",
    "jest": "^30.2.0",
    "rimraf": "^5.0.5",
    "ts-jest": "^29.4.5",
    "turbo": "^1.13.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "zod": "^4.1.12"
  }
}
