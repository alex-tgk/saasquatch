# {{project.name}} - Infrastructure Quick Start

## 1-Minute Setup

```bash
# Copy environment template
cp .env.example .env

# Edit .env and set JWT_SECRET (required!)
# Generate a secure secret: openssl rand -base64 32

# Start everything
make up

# Check health
make health
```

## Essential Commands

```bash
make up        # Start all services
make down      # Stop all services
make logs      # View logs
make health    # Check service health
make ps        # Show status
```

## Service URLs

{{#each services}}
- {{this.name}}: http://localhost:{{this.port}}/health
{{/each}}

{{#if infrastructure.database}}
{{#eq infrastructure.database.type "postgresql"}}
## Database Access

```bash
# Open PostgreSQL shell
make db-shell

# Create backup
make db-backup

# Restore backup
make db-restore FILE=backups/backup.sql
```
{{/eq}}
{{/if}}

## Troubleshooting

### Ports already in use
```bash
# Check what's using a port
lsof -i :{{#if services}}{{services.[0].port}}{{else}}3000{{/if}}

# Or change ports in .env file
```

### Services won't start
```bash
# Check logs
make logs

# Rebuild everything
make rebuild

# Nuclear option (deletes data!)
make clean && make up
```

### Database connection failed
```bash
# Check PostgreSQL is running
docker-compose ps postgres

# Check logs
docker-compose logs postgres

# Verify credentials in .env match docker-compose.yml
```

## Next Steps

1. Read full documentation: [README.md](./README.md)
2. Check service-specific docs in `../services/*/README.md`
3. Visit Swagger docs at http://localhost:3001/docs (auth service)

## Need Help?

- All commands: `make help`
- Infrastructure docs: [README.md](./README.md)
- Project docs: [../docs/](../docs/)
